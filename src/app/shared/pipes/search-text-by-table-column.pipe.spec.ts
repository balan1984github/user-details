import { FilterPipe } from './search-text-by-table-column.pipe';

describe('SearchTextByTableColumnPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPipe();
    expect(pipe).toBeTruthy();
  });
});
